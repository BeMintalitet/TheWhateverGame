<?xml version="1.0" encoding="utf-8"?>
<!-- The game stores everything locally and deliberately does NOT back it up:
     a restored save on a new device would carry a stale advertising context and
     there is nothing here worth syncing. Keeps the Data Safety answer honest. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="file" />
    </device-transfer>
</data-extraction-rules>
